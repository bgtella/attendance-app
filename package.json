{
  "name": "solpg-attendance-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && node -e \"require('gh-pages').publish('dist', {repo:'https://github.com/bgtella/attendance-app.git',branch:'gh-pages',message:'Deploy: update build'}, (err)=>{ if(err){console.error(err);process.exit(1)}else{console.log('Published!')} })\""
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.11",
    "@vitejs/plugin-react": "^4.3.1",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^4.1.11",
    "vite": "^6.3.5"
  }
}
